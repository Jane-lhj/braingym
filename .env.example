# Copy to .env and fill in your key (never commit .env)
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# Optional: Docker Compose 使用 ./data 目录持久化数据库（compose 已默认注入，一般无需手写）
# DATABASE_URL=sqlite:////data/braingym.db
